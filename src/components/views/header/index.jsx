import React from "react";

import { HeaderContainer, TitleText, SearchBar, SearchInput } from "./styles";

export const Header = () => (
  <HeaderContainer>
      <TitleText>findIT</TitleText>
      <SearchInput/>
  </HeaderContainer>
);
